name: Update OffBeat feed

on:
  workflow_dispatch:
  schedule:
    - cron: '23 */3 * * *'

permissions:
  contents: write

jobs:
  build-feed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Generate feed
        run: python generate_feed.py --out docs/feed.xml
      - name: Commit updated artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/feed.xml docs/events.json
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Update feed + events JSON"
            git push
          fi
